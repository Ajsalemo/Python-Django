build:
  docker:
    web: Dockerfile
setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE    
run:
  web: gunicorn pythondjango.wsgi:application --bind 0.0.0.0:$PORT
release:
  image: web


