setup:
    addons:
      - plan: heroku-postgresql:hobby-dev
        as: DATABASE
build:
    docker:
        web: Dockerfile
        worker: worker/Dockerfile
release:
    command:
        - ./deployment-tasks.sh
    image: worker
